import { FiArrowRight } from "react-icons/fi";

const HeroButton = () => {
  return (
    <button className="hero-button">
      See my work
      <FiArrowRight />
    </button>
  );
};

export default HeroButton;
