import React from "react";
import Button from "../../components/Button/Button";

const Blog = () => {
  return (
    <div>
      <Button to="/" text="go back" direction="right" />
    </div>
  );
};

export default Blog;
